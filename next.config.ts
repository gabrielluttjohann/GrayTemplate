// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placehold.co", "via.placeholder.com", "picsum.photos"], // Adicione os domínios que você precisa
  },
};

module.exports = nextConfig;
